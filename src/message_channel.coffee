# A special type of service which listens to changes from deputies on other
# application instances open in the same browser.
#
# TODO: build as part of angular-deputy, as changes will need to be run inside
# an $apply